UCString(len)
---


Creates a string from the memory pointed to by the pointer object, regardless of its type.
If no _len_ is given, the string is required to be zero-terminated. _len_ refers
to the number of characters, not the number of bytes.

Characters are 16 bits wide.

See also
---


[[Pointer.prototype.string]]
[[Pointer.prototype.UTF8String]]
